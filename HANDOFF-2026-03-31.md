# Handoff — March 31, 2026

## What Was Done Today

### 1. Pull-Based Streaming Workers (v40) — Deployed & Running
- **WorkerLoop** VirtualObject live on production — workers continuously pull from Postgres queue
- Worker A has been processing cases successfully (reached L1_REVIEW status)
- Replaced batch dispatch with streaming pull architecture

### 2. Worker B & C Credential Issues — Diagnosed & Partially Fixed
- **Root cause**: Accounts got locked by Carelon portal due to failed login attempts with old passwords in DB
- User deleted/recreated worker accounts in frontend with new passwords
- **Missing `worker_url`**: Frontend create form didn't have a worker_url field → workers B & C had `worker_url=NULL` in DB → WorkerSession couldn't reach them
- **Fixed**: Set worker_urls via direct DB update (B=`http://10.0.0.7:9081`, C=`http://10.0.0.6:9081`)

### 3. Worker C — CONFIRMED WORKING ✅
- Successfully logged in, went through full portal flow (member search → eligibility → DI select → start order → existing auths → provider search)
- Hit a data issue on one case: "no providers linked to member's health plan" → auto-requeued
- **Currently deactivated** — ready to re-activate for streaming

### 4. Worker B — STILL LOCKED ❌
- Okta-level lockout — password field never appears after entering username
- Portal-level unlock was done by user, but Okta IdP lockout persists
- **Deactivated and stop_requested=True** to prevent further login attempts
- Screenshot saved at `/tmp/password_field_not_found.png` on worker-b VM
- **Action needed**: Clear Okta lockout (may need Carelon support or self-service password reset)

### 5. Frontend v32 Deployed — Worker Edit UI
- Added **inline Edit/Save** for worker accounts (username, password, worker_url, mailbox, shift)
- Added **worker_url field** to the Add Worker form
- Workers table now shows worker_url column (red "NOT SET" if missing)
- API type updated in `frontend/lib/api.ts`

### 6. 22 Login-Failure Cases Re-queued
- All HOLD cases from login failures reset to NOTES_UPLOADED + QUEUED
- Only 1 pre-existing HOLD case remains

## Current Production State

| Worker | Status | Notes |
|--------|--------|-------|
| vm-worker-a | Active, running | Processing cases, browser alive |
| vm-worker-b | **Deactivated**, stopped | Okta lockout — needs unlock |
| vm-worker-c | **Deactivated**, confirmed working | Ready to re-activate for streaming |

### Case Counts (as of ~05:15 UTC)
- PENDING_NOTES: 253 (extraction still running)
- PENDING_STAT: 13
- NOTES_UPLOADED: 121+ (ready for processing)
- L1_REVIEW: 1
- HOLD: 1

### DB Credentials
- vm-worker-a: password empty (falls back to env var `CARELON_PASSWORD`) — working
- vm-worker-b: password in DB (13 chars, T...!) — account locked at Okta
- vm-worker-c: password in DB (13 chars, T...$) — confirmed working
- Mailboxes: carelon-mfa@revelooptechsystems.com (A), carelon-mfb@ronexa.com (B), carelon-mfc@ronexa.com (C)
- Graph API confirmed working for all mailboxes

## Next Steps

1. **Re-activate Worker C** for full streaming (unlimited cases)
   ```
   # On VM via az vm run-command:
   # Activate in DB, then start WorkerLoop
   curl -X POST http://localhost:8080/WorkerLoop/vm-worker-c/start/send -H "Content-Type: application/json" -d '{}'
   ```

2. **Fix Worker B Okta lockout**
   - Contact Carelon or use self-service to unlock the Okta account
   - Once unlocked, update password in DB via Settings UI (Edit button now works)
   - Re-activate and test with `max_cases=1, stop_on_empty=true` first

3. **Monitor queue drain** — with 2-3 workers streaming, 121+ cases should clear steadily

4. **CI/CD + Git** — still pending from earlier request

## Files Changed Today

### Frontend
- `frontend/app/settings/page.tsx` — Added worker edit UI, worker_url field in create form
- `frontend/lib/api.ts` — Added `worker_url` to createWorkerAccount type

### Backend (from previous session, deployed as v40)
- `backend/app/workflow/worker_loop.py` — NEW: WorkerLoop VirtualObject
- `backend/app/db/models.py` — Added stop_requested, wake_awakeable_id to WorkerAccount
- `backend/app/api/routes/settings.py` — start/stop/status worker endpoints
- `backend/app/ingest/sync_engine.py` — Wake sleeping workers + stale reaper
- `backend/app/worker/helpers.py` — Removed batch counter
- `backend/app/worker/http_server.py` — Removed batch endpoints
- `backend/app/workflow/worker_session.py` — Removed set_batch_size
- `backend/restate_worker.py` — Registered WorkerLoop
- `backend/tests/conftest.py` — NEW: Test fixtures
- `backend/tests/test_queue.py` — NEW: Queue tests
- `backend/tests/test_worker_loop.py` — NEW: Worker loop tests

## Key Learnings
- Frontend had no Edit UI for workers — only Add/Delete/Toggle. Now fixed.
- Create form was missing `worker_url` field — workers created via UI had NULL urls
- Carelon portal lockout ≠ Okta IdP lockout — need to clear both
- `password or env_var` fallback: empty string is falsy in Python, so worker-a with empty DB password correctly falls back to env var
